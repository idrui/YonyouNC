package nc.ui.pu.pub.editor.card.listener;

import nc.ui.pubapp.uif2app.event.card.CardBodyBeforeEditEvent;

/**
 * <p>
 * <b>本类主要完成以下功能：</b>
 * <ul>
 * <li>处理卡片表体编辑前触发的事件
 * </ul>
 * <p>
 * <p>
 * 
 * @version 6.0
 * @since 6.0
 * @author hanbin
 * @time 2010-1-24 下午05:04:10
 */
public interface ICardBodyBeforeEditEventListener extends ICardEventListener {

  /**
   * 处理卡片表体编辑前触发的事件
   * <p>
   * <b>注意：由于平台原因，只有表体编辑前可以用来阻止编辑，表头不能使用此方法来阻止编辑</b>
   */
  public void beforeEdit(CardBodyBeforeEditEvent event);
}
